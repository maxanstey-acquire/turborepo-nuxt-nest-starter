{
  "name": "@app/frontend",
  "version": "0.0.0",
  "private": true,
  "dependencies": {
    "@orpc/client": "^1.11.3",
    "@app/contracts": "workspace:*",
    "@app/shared": "workspace:*",
    "@tailwindcss/postcss": "^4.1.17",
    "tailwindcss": "^4.1.17"
  },
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt generate",
    "lint": "eslint .",
    "fix": "eslint . --fix",
    "check": "nuxt typecheck",
    "check-tsc": "vue-tsc --strict -p tsconfig.json --noEmit",
    "prepare": "nuxt prepare"
  },
  "turbo": {
    "outputs": [".nuxt/**", ".output/**"]
  }
}
